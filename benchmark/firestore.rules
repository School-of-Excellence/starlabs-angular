rules_version = '2';
// EMULATOR-ONLY test rules. Wide open so the seed/bench scripts can write/read
// freely against the local in-memory emulator. Never deploy these.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
